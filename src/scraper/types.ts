export interface PinSummary {
  title: string;
  url: string;
}

export interface PinDetails {
  title: string;
  url: string;
  creator: string;
  board: string;
  likes: string;
  comments: string;
  saves: string;
  shares: string;
}
